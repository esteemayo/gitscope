export interface ContributionItem {
  id: string;
  title: string;
  description: string;
  accentColor: string;
}

export interface ContributionTimelineType {
  label: string;
  value: string;
  width: string;
  accentColor: string;
}

export interface ActivityItemType {
  id: string;
  label: string;
  description: string;
  accentColor: string;
}

export interface ReadingType {
  label: string;
  description: string;
  accentColor: string;
}

export interface contributionStepType {
  id: string;
  title: string;
  description: string;
}
