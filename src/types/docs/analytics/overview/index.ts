export interface AnalyticsItemType {
  id: string;
  title: string;
  description: string;
  accentColor: string;
  items: string[];
}

export interface AnalyticsStepType {
  title: string;
  description: string;
  accentColor: string;
}

export interface AnalyticsReadingType {
  id: string;
  title: string;
  description: string;
  accentColor: string;
}
