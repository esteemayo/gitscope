import { LucideIcon } from 'lucide-react';

export interface AboutStatCardProps {
  icon: LucideIcon;
  value: string;
  label: string;
  trend: string;
  trendLabel: string;
  accentColor: string;
  index: number;
}
