import { AnalyticsMethodItemType } from './index';

export interface AnalyticsMethodologyItemProps extends AnalyticsMethodItemType {
  index: number;
  className?: string;
  style?: React.CSSProperties;
}
