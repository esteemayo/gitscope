import { quickStartCards } from './quickStart.data';
import { quickStartContent } from './quickStartContent.data';

import { QuickStartDataType } from '@/types/docs/docsCard';

export const quickStartData: QuickStartDataType = {
  content: quickStartContent,
  cards: quickStartCards,
};
