import { DocsHomeNextType } from '@/types/docs/docsCard';
import { nextStepsContent } from './nextStepsContent.data';

export const nextStepsData: DocsHomeNextType = {
  ...nextStepsContent,
};
